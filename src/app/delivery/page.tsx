import { DeliveryList } from "../_components/DeliveryList";

export default function Delivery() {
  return (
    <section className="h-screen bg-gradient-to-r from-green-600 to-blue-500">
      <DeliveryList />
    </section>
  );
}
